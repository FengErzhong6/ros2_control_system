# Data Collection Stack

This directory hosts the top-level data collection stack described in
`IMPLEMENTATION_PLAN.md`.

Phase 0 goals:

- create the package boundaries
- provide placeholder interfaces
- provide a minimal supervisor bringup path
- keep all existing device packages standalone and unchanged

Current package layout:

- `data_collection_interfaces`
- `data_collection_orchestrator`
- `data_collection_ui`
- `data_collection_bringup`
